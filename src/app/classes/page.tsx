import { AppLayout } from "../../components/ui/app-layout";import { OptionList } from "../../components/compendium/option-list";import { requireUser } from "../../server/auth/session";import { retrievalUser } from "../../server/compendium/http";import { parseOptionListOptions } from "../../server/compendium/option-http";import { OptionReadService } from "../../server/compendium/option-read-service";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const user=await requireUser();const url=new URL("http://local/classes");for(const [key,value] of Object.entries(await searchParams))for(const item of Array.isArray(value)?value:value?[value]:[])url.searchParams.append(key,item);const options=parseOptionListOptions("class",url);const result=await new OptionReadService().list("class",retrievalUser(user),options);return <AppLayout userRole={user.role} wide><OptionList type="class" options={result.options} count={result.count} query={options.query} kind={options.kind}/></AppLayout>}

import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import test from "node:test";import {MIGRATION_FILENAMES} from "../../src/server/db/migrations.ts";
const filename="0019_class_species_hierarchy.sql";const sql=await readFile(`migrations/${filename}`,"utf8");
test("0019 registers normalized version-owned hierarchy and progression",()=>{assert.equal(MIGRATION_FILENAMES.at(-1),filename);for(const table of ["compendium_class_parent_links","compendium_species_parent_links","compendium_class_progression_tables","compendium_class_progression_columns","compendium_class_progression_rows","compendium_class_progression_cells","compendium_class_feature_links","compendium_species_traits","compendium_option_cross_links"])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}`));assert.doesNotMatch(sql,/parent_(?:class|species)_id\s+uuid/);assert.match(sql,/level BETWEEN 1 AND 20/);assert.match(sql,/exactly once/);});
